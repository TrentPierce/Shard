{
  "buildCommand": "cd web && npm run build && cd .. && rm -rf .next && cp -R web/.next .next && cp web/package.json package.json",
  "installCommand": "cd web && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://54.80.206.54.nip.io/:path*"
    }
  ]
}
