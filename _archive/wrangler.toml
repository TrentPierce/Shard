# Cloudflare Pages build configuration
# Build only the web directory for Next.js

name = "shard-web"
compatibility_date = "2026-02-27"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run deploy"
cwd = "web"
