FROM python:3.12-slim

WORKDIR /app

COPY integrations/requirements.txt .
RUN pip install -r requirements.txt --no-cache-dir

COPY integrations/ integrations/

CMD ["uvicorn", "integrations.primary_backend:app", "--host", "0.0.0.0", "--port", "8000"]
